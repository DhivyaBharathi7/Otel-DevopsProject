apiVersion: v1
kind: Service
metadata:
  name: payment
  
  labels:
    app: payment
    component: paymentservice
spec:
  type: ClusterIP
  ports:
  - port: 8085
    targetPort: 8085
    name: http
  selector:
    app: payment