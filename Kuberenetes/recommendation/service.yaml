apiVersion: v1
kind: Service
metadata:
  name: recommendation
  
  labels:
    app: recommendation
    component: recommendation
spec:
  selector:
    app: recommendation
  ports:
  - name: http
    port: 8087
    targetPort: 80
    protocol: TCP
  type: ClusterIP